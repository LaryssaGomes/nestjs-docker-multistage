name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  build:
    name: Build Docker image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - uses: actions/setup-node@v3
      - name: Setup node
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: '**/package-lock.json'
      - run: npm install
      - run: npm test
      - name: Generate tag
        id: generate_tag
        run: |
          SHA=$(echo $GITHUB_SHA | head -c7)
          echo "tag=$SHA" >> $GITHUB_OUTPUT
  
      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push 
        uses: docker/build-push-action@v5
        with:
          push: true
          tags: lurye/rocketseat-ci-api:${{ steps.generate_tag.outputs.tag }}, lurye/rocketseat-ci-api:latest

#      - name: Build Docker image
#        run: docker build -t lurye/rocketseat-ci-api:${{ steps.generate_tag.outputs.tag }} .
#
#      - name: Push image
#        run: docker push lurye/rocketseat-ci-api:${{ steps.generate_tag.outputs.tag }}